<h1>Feed</h1>

<% @feed_content.each do |dog_content| %>
  <div>
    <%= render "shared/contentshow", content: dog_content, type: dog_content.content_type, comments: dog_content.comments %>
  </div>
<% end %>