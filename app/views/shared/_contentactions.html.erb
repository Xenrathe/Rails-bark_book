<div>
  <% if current_user && current_user.id == content.user_id %>
    <%= link_to "Edit this post", [:edit, content] %> | 
    <%= link_to "Delete #{type} post", content, method: :delete, data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this #{type} post?"} %> | 
  <% end %>

  <%= link_to "Back to dogs", dogs_path %>  
</div>